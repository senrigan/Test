package com.gdc.nms.test;

import java.io.File;
import java.util.concurrent.TimeUnit;

import com.gdc.nms.tester.client.Client;

public class Main {
	public static void main(String[] args) {
//		Client client=new Client("192.168.208.70");
//		client.setServerTesterFile(new File("C:\\Users\\senrigan\\wnms\\release\\release\\server\\tester-server.jar"));
//		client.setServers(null);
//		client.start(1,TimeUnit.MINUTES);
//		client.getDeviceResources(device, receiver, timeout);
//		client.getDeviceResourceStats(device, resource, receiver, timeout);
//		client.getDeviceResourceStats(device, iface, receiver, timeout);
//		client.getDeviceResourceStats(device, resource, receiver, timeout)
//		client.setServers(servers);
		
	}
}
